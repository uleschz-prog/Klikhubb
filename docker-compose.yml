services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: klikhubb
      POSTGRES_PASSWORD: klikhubb
      POSTGRES_DB: klikhubb
    ports:
      - "5432:5432"
    volumes:
      - klikhubb_pg:/var/lib/postgresql/data

volumes:
  klikhubb_pg:
